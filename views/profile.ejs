<!doctype html>
<html>
<head>
    <title>HOUSE CASS CAFE</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">

    <script src="https://kit.fontawesome.com/b1767921c9.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="profilestyle.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa solid fa-arrow-down"></span>order</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

  
    
    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div>
          <section>
            <% if (user.local.email) { %>
              <p>
                <strong>NAME</strong>: <%= user.local.email %><br> 
            </p>
        <% } else { %>
            <a href="/connect/local" class="btn btn-default">Connect Local</a>
        <% } %>
          </section>
        
        </div>
        <div class="col-sm-6" id ="orders">
         
          <h3 class ="general">PENDING orders</h3>

           <ul class="orders">
            <% for(let i=0; i<orders.length; i++) {%>
             
                <% if(orders[i].completed===false){ %>
                <li class="message">  
                  <span id="displayUrl"><%= orders[i].customerName %></span>
                  <span id="displayUrl"><%= orders[i].coffeeChoice %></span>
                  <span id="displayUrl"><%= orders[i].coffeeChoice2 %></span>
                  <span id="displayUrl"><%= orders[i].coffeeChoice3 %></span>
                  <span id="displayUrl"><%= orders[i].coffeeChoice4 %></span>
                  <span id="displayUrl"><%= orders[i].coffeeChoice5 %></span>
                  <span id="bookCover"><%= orders[i].coffeeInstructions %></span>
                  <span id="displayUrl" hidden><%= orders[i]._id %></span>
                  <span><i class="fa-solid fa-heart" aria-hidden="true"></i></span>
                  <span><i class="fa fa-ban" aria-hidden="true"></i></span>
                </li>
            <% } %>
            <% } %>
  

            </ul>
            </div>

<!-- WE NEED IT TO LOOK LIKE A POINT OF SALES-- GOOGLE THAT AND YOU ALSO NEED TO SPLIT UP THE ordersS WHEN THEY'RE COMPLETED INTO THEIR RESPECTIVE CATEGORIES SO THAT  -->
  
      
          <div class="col-sm-6" id="orders">
            <h3 class ="liked">COMPLETED</h3>
            <ul class="orders">
              <% for(let i=0; i < orders.length; i++){%>
                <% if(orders[i].completed ===true) { %>
                  <li class="message">
                    <span>Customer Name : </span><span id="displayUrl"> <%= orders[i].customerName %></span>
                    <span>Coffee Choice : </span><span id="displayUrl"><%= orders[i].coffeeChoice %></span>
                    <span>Coffee Choice2 : </span><span id="displayUrl"><%= orders[i].coffeeChoice2 %></span>
                    <span>Coffee Choice3 : </span><span id="displayUrl"><%= orders[i].coffeeChoice3 %></span>
                    <span>Coffee Choice4 : </span><span id="displayUrl"><%= orders[i].coffeeChoice4 %></span>
                    <span>Coffee Choice5 : </span><span id="displayUrl"><%= orders[i].coffeeChoice5 %></span>
                    <span>Coffee Instructions : </span><span id="bookCover"><%= orders[i].coffeeInstructions %></span>
                    <span>Completed By :</span><span id="displayBarista" value ="baristaName"> <%= orders[i].baristaName %></span>
                    <span><i class="fa fa-ban" aria-hidden="true"></i></span>
                  </li>
                  <% } %>
                <% } %>
                
            </ul>
          </div>
          </div>
      
          <div class="col-sm-6" id="orders">
         

            <ul class="orders">
              <% for(var j=0; j<allUsers.length; j++) {%> 
              <% console.log(orders)%> 
              <% if (orders.filter (x => x.baristaName === allUsers[j].local.email).length > 0) {%> 
                <h3 class ="liked"> <%= allUsers[j].local.email %> orders</h3>   
                <% } %>
                <% for(let i=0; i<orders.length; i++) {%>
                  <% if(orders[i].completed===true && allUsers[j].local.email=== orders[i].baristaName){ %>
                              <li>Customer: <%=orders[i].customerName %></li>
                              <li>orders: <%=orders[i].coffeeChoice %></li>
                              <li>orders: <%=orders[i].coffeeChoice2 %></li>
                              <li>orders: <%=orders[i].coffeeChoice3 %></li>
                              <li>orders: <%=orders[i].coffeeChoice4 %></li>
                              <li>orders: <%=orders[i].coffeeChoice5 %></li>
                              <li>Coffee Instructions: <%=orders[i].coffeeInstructions %></li>
                              <li>Barista: <%=orders[i].baristaName %></li>
              <% } %>
              <% } %>  
              <% } %>
          </ul>
            
          </div>

         

</div>

<script src="main.js"></script>

</body>
</html>
